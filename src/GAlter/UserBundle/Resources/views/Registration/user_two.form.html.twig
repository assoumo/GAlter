{% extends "GAlterUserBundle::layout.html.twig" %}

{% block fos_user_content %}
    {% trans_default_domain 'FOSUserBundle' %}
    formulaire reponsable
    <form action="{{ path('user_two_registration') }}" {{ form_enctype(form) }} method="POST">

        {#{{ form_widget(form.username,{'attr':{'class':'form-control','placeholder':'Votre nom / prenom'}}) }}#}
        {{ form_widget(form) }}
        <div>
            <input type="submit" value="{{ 'registration.submit'|trans }}" />
        </div>
    </form>
{% endblock fos_user_content %}