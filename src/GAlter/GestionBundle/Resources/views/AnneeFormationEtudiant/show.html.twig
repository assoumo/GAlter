{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AnneeFormationEtudiant</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Anneeformation</th>
                <td>{{ entity.anneeFormation }}</td>
            </tr>
            <tr>
                <th>Estvalide</th>
                <td>{{ entity.EstValide }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('anneeformationetudiant') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('anneeformationetudiant_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
