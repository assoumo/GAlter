{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Options_compte list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Idparametre</th>
                <th>Alerteactive</th>
                <th>Periode</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('options_compte_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idParametre }}</td>
                <td>{{ entity.alerteActive }}</td>
                <td>{{ entity.periode }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('options_compte_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('options_compte_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('options_compte_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
